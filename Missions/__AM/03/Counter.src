Every 0$01 trigger cornel_prepared do
begin
enable;
Display_Strings := ['#Am03-1', cornel_counter];
cornel_counter := cornel_counter - 0$01;

if cornel_counter = 0$00 then
   begin
   Display_Strings := [];
   disable;
   end;
End;