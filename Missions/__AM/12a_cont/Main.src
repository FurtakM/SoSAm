Starting
begin
ResetFog;

FogOff(7);


InitVariables;
PrepareAlliance;
PrepareFriends;
PrepareAmerican;

if debug then
   begin
   SetAttitude(1, 7, att_enemy, true);
   RemoveUnit(Powell);
   end
  else
   Action;
End;

Export debug, selected, friends, others;
Export Function InitVariables;
begin
debug := 1;
friends := [];
selected := [];
End;

